{
	"extends": "../tsconfig.base.json",
	"compilerOptions": {
		"outDir": "dist",
		"rootDir": ".",
		"baseUrl": ".",
		"paths": {
			"~/*": ["bot-layer/*"]
		}
	},
	"include": ["core", "bot-layer", "permissions"],
	"exclude": ["**/*.test.ts", "**/*.bench.ts", "**/__tests__/**"]
}
